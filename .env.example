# Copier en `.env` puis ajuster si besoin.

# Base de données SQLite (fichier local — aucun serveur à installer).
# Migration Postgres : provider = "postgresql" + postgresql://...
DATABASE_URL="file:./dev.db"

# Secret de signature des jetons de session (à régénérer en production).
AUTH_SECRET="change-me-in-production"
